version: '3.8'

services:
  emotion-recognition:
    build: .
    container_name: facial_emotion_platform
    ports:
      - "8501:8501"
    volumes:
      - .:/workspace
    environment:
      - MLFLOW_TRACKING_URI=http://localhost:5000
    restart: unless-stopped
